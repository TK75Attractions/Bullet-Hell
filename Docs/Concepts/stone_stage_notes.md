# Stone Stage Notes

## Track

- Title: 石工
- Composer: べにぐち
- Source file: `C:\Users\battl\Downloads\何かのボスに　サンプル.m4a`
- Local copied source: `Assets/StageData/stone/stone.m4a`
- Existing playable stage audio: `Assets/StageData/stone/stone.mp3`

## Timing

- Existing `Assets/StageData/stone/stone.json` is already set to `BPM: 144`, `measure: 4`, `barCount: 91`.
- 1 beat = 0.4167 sec.
- 1 bar = 1.6667 sec.
- Key phrase starts align to 8-bar blocks:
  - 00:06.67 = bar 5 start
  - 00:20.00 = bar 13 start
  - 00:33.33 = bar 21 start
- The user's rough 00:06 / 00:20 / 00:34 plan matches this 144 BPM grid.

## Attack Concept

- 00:06: Random blocks appear, slam downward together, then conveyor carries them left. Repeat twice.
- 00:20: Random blocks fall from above, slam downward together, then conveyor carries them left. Repeat twice.
- 00:34 onward: undecided.

Concept image:

- `Docs/Concepts/stone_stage_attack_concept.png`

## Current Project State

- `Assets/StageData/stone/stone.json` already exists and contains a full generated stage for 石工.
- It has 383 bullet spawners and no enemy spawners.
- Early timings include dense events around 6.67s, 20.00s, and 33.33s.
- `stageDescription` has been changed from `Generated by 弾幕メーカー` to `べにぐち` so the UI composer row is correct.
- Stage order is currently implicit: `StageDataManager` uses the folder enumeration order from `Assets/StageData` without a separate sort/index field. In the current workspace listing, `stone` appears after `mirror`, so it is the sixth folder if counted 1-based. If "fifth stage" means a 1-based UI position, the loader or folder/order policy needs an explicit adjustment.

## Audio Import Note

Unity imported `Assets/StageData/stone/stone.m4a` as `UnityEditor.DefaultAsset`, not `UnityEngine.AudioClip`.
The current official-stage loader will therefore continue to rely on `stone.mp3` for playable audio unless the audio is converted to a Unity-supported imported format or the loading path is changed.

## Google Drive

The connected Google Drive account did not return the requested folder names:

- `弾幕ゲームスプライト`
- `弾幕ゲーム：石工`
- `弾幕`
- `石工`

Broader shared-folder and all-folder searches also did not show the target folder. A direct Drive folder URL or sharing the folder with the connected account should unblock sprite import.
